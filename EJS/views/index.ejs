<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="public/main.css">
</head>
<body>
    <h1>안녕하세요</h1>
    <!-- <p><%#= message%></p> -->
    <p><%= "Hello" + "World" %></p>
    <p><%= 10 - 2%></p>
    <p><%= true ? "참" : "거짓" %></p>
    <div>
        <% let str = "hello" %>
        <%= str %>
    </div>
    <div>
        <% if(true){ %>
            <h2>출력</h2>
        <% } %>
    </div>
    <div>
        <% for(let i = 1; i <= 3; i ++) { %>
            <div>
                <a href="/order/ <%= i %>">test <%= i %> 로 </a>
            </div> 
        <% } %>
    </div>
    <div>
        <% let arr = ["apple", "banana", "cherry"] %>
        <% arr.forEach((fruit) => {%>
            <h3><%= fruit %> 입니다 </h3>
        <% }) %>
    </div>
    <div>
        <% let arr2 = ["m1", 'm2', 'm3'] %>
        <% arr2.map((val) => { %>
            <h3><%= arr2 %> 입니다 </h3>
        <% }) %>    
    </div>
    <ul>
        <!-- <#% for(let person of people) { %> -->
            <!-- <li><%#= person.name %></li> -->
            <li><%= people[0].name %></li>
        <%# } %>
    </ul>

    <ul>
        <% users2.forEach(user => { %>
            <li>
                name : <%= user.name %> / age: <%= user.age %> 
                <%= user.age >= 18 ? 'pass' : 'reject' %> - <%= user.role %>
            </li>
        <% }) %>
    </ul>
</body>
</html>